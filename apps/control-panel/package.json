{
  "name": "@uo/control-panel",
  "version": "1.0.0",
  "private": true,
  "description": "Electron desktop launcher for the UO-Node stack — start/stop server, client, admin, bridge from one window with tabbed logs.",
  "main": "./src/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test ./test/*.test.mjs",
    "smoke": "node ./test/electron-smoke.cjs"
  },
  "devDependencies": {
    "electron": "^43.2.0"
  }
}
