// World map constants. The actual block + statics data is fetched via
// `assets.fetchBlock(cx, cy)` / `assets.fetchStatics(cx, cy)` in
// src/assets/asset-manager.js — the synthetic placeholder generator from
// PHASE 1 was removed once the extractor went online.

export const CHUNK_SIZE = 8;
